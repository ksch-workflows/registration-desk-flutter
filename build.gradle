apply plugin: 'java'
apply plugin: 'maven-publish'

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId = 'ksch-workflows'
            artifactId = 'registration-desk'
            version = '0.0.7'

            from components.java
        }
    }
}
